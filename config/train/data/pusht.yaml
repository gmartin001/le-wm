dataset:
  cache_dir: /Volumes/BCKUP-Greg-MacBookPro/4MP/World_Models/.stable_wm/datasets
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state